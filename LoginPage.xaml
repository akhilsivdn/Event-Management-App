<Page
    x:Class="Events.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Events"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#0D223F">
        <Image  Source="Assets/Artboard 1.png" Stretch="UniformToFill"/>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,220,0,0" Width="360" >
            <TextBox PlaceholderText="Student ID" Foreground="#0D223F" x:Name="studId"  Margin="15" BorderBrush="#0D223F" Background="#66FFFFFF"/>
            <PasswordBox PlaceholderText="Password" Foreground="#0D223F" x:Name="password" IsPasswordRevealButtonEnabled="True" Margin="15" BorderBrush="#0D223F"/>
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,450,0,0" Orientation="Horizontal">
            <Button Content="BACK" Width="140" Margin="15" Click="BackButton_Click"/>
            <Button Content="LOGIN" FontSize="20" Width="140" Margin="15"  Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Page>
